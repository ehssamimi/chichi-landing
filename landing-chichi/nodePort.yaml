apiVersion: v1
kind: Service
metadata:
  name: chichi_landing
spec:
  selector:
    app: chichi_landing
  ports:
    - port: 3000
      targetPort: 3800
      nodePort: 38030
      protocol: TCP
      name: chichilanding
  type: NodePort